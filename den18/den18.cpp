for (size_t i = 0; i < 99; i++)
{
    scitanie();
    int zmena = 1;
    while (zmena == 1)
    {
        zmena = explooosion();
        znaky.shrink_to_fit();
        if (zmena == 0)
        {
            zmena = split();

        }
    }

}